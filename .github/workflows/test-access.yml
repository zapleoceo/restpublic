name: Test Workflow Access

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - name: Test step
      run: |
        echo "✅ Workflow access test successful!"
        echo "Current time: $(date)"
        echo "GitHub Actions runner: $RUNNER_OS"
