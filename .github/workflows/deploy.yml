name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Debug secrets
      run: |
        echo "Checking if secrets are available..."
        if [ -n "${{ secrets.PRODUCTION_HOST }}" ]; then
          echo "✅ PRODUCTION_HOST is set"
        else
          echo "❌ PRODUCTION_HOST is missing"
          exit 1
        fi
        
        if [ -n "${{ secrets.PRODUCTION_USER }}" ]; then
          echo "✅ PRODUCTION_USER is set"
        else
          echo "❌ PRODUCTION_USER is missing"
          exit 1
        fi
        
        if [ -n "${{ secrets.PRODUCTION_SSH_KEY }}" ]; then
          echo "✅ PRODUCTION_SSH_KEY is set"
        else
          echo "❌ PRODUCTION_SSH_KEY is missing"
          exit 1
        fi
      
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.PRODUCTION_HOST }}
        username: ${{ secrets.PRODUCTION_USER }}
        key: ${{ secrets.PRODUCTION_SSH_KEY }}
        port: 22
        timeout: 60s
        command_timeout: 60m
        script: |
          cd /var/www/northrepubli_usr/data/www/northrepublic.me
          echo "🚀 Начинаем автодеплой Production v5.0..."
          echo "📅 Время: $(date)"
          echo "📁 Директория: $(pwd)"
          echo "🔧 Проверяем Git статус..."
          git status
          
          # Проверяем права на скрипт
          chmod +x deploy.sh
          
          # Выполняем деплой с подробным логированием
          ./deploy.sh
          
          echo "✅ Автодеплой Production v5.0 завершен!"
          
    - name: Health check
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.PRODUCTION_HOST }}
        username: ${{ secrets.PRODUCTION_USER }}
        key: ${{ secrets.PRODUCTION_SSH_KEY }}
        port: 22
        timeout: 60s
        command_timeout: 10m
        script: |
          echo "🔍 Выполняем health check..."
          sleep 20
          
          # Проверяем PM2 процессы
          echo "🔍 Проверяем PM2 процессы..."
          pm2 list
          
          # Проверяем backend
          echo "📡 Проверяем backend API..."
          for i in {1..5}; do
            echo "Попытка $i из 5..."
            if curl -f http://localhost:3002/api/health; then
              echo "✅ Backend работает!"
              break
            else
              echo "❌ Попытка $i не удалась"
              if [ $i -eq 5 ]; then
                echo "❌ Backend не отвечает после 5 попыток"
                echo "📋 Логи PM2:"
                pm2 logs northrepublic-backend --lines 10 || echo "❌ PM2 логи недоступны"
                exit 1
              fi
              sleep 5
            fi
          done
          
          # Проверяем MongoDB
          echo "🔗 Проверяем MongoDB..."
          if curl -f http://localhost:3002/api/admin/stats; then
            echo "✅ MongoDB работает!"
          else
            echo "❌ MongoDB не отвечает"
            exit 1
          fi
          
          # Проверяем frontend
          echo "🌐 Проверяем frontend..."
          if curl -f https://northrepublic.me | grep -q "North Republic"; then
            echo "✅ Frontend работает!"
          else
            echo "❌ Frontend не отвечает"
            exit 1
          fi
          
          echo "🎉 Все проверки пройдены успешно!"
          
    - name: Notify success
      if: success()
      run: |
        echo "✅ Деплой успешно завершен!"
        echo "🌐 Сайт: https://northrepublic.me"
        echo "🔧 Админ панель: https://northrepublic.me/admin"
        
    - name: Notify failure
      if: failure()
      run: |
        echo "❌ Деплой завершился с ошибкой!"
        echo "Проверьте логи выше для деталей"
