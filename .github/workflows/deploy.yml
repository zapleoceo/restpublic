# Updated: 2025-08-31 - Improved deployment workflow
name: Deploy to Production

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Debug secrets
      run: |
        echo "Checking if secrets are available..."
        if [ -n "${{ secrets.PRODUCTION_HOST }}" ]; then
          echo "✅ PRODUCTION_HOST is set"
        else
          echo "❌ PRODUCTION_HOST is missing"
          exit 1
        fi
        
        if [ -n "${{ secrets.PRODUCTION_USER }}" ]; then
          echo "✅ PRODUCTION_USER is set"
        else
          echo "❌ PRODUCTION_USER is missing"
          exit 1
        fi
        
        if [ -n "${{ secrets.PRODUCTION_SSH_KEY }}" ]; then
          echo "✅ PRODUCTION_SSH_KEY is set"
        else
          echo "❌ PRODUCTION_SSH_KEY is missing"
          exit 1
        fi
      
    - name: Deploy to server
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.PRODUCTION_HOST }}
        username: ${{ secrets.PRODUCTION_USER }}
        key: ${{ secrets.PRODUCTION_SSH_KEY }}
        port: 22
        timeout: 60s
        command_timeout: 60m
        script: |
          cd /var/www/northrepubli_usr/data/www/northrepublic.me
          echo "🚀 Начинаем автодеплой Production v5.1..."
          echo "📅 Время: $(date)"
          echo "📁 Директория: $(pwd)"
          echo "🔧 Проверяем Git статус..."
          git status
          
          # Проверяем права на скрипт
          chmod +x deploy.sh
          
          # Выполняем деплой с подробным логированием
          ./deploy.sh
          
          echo "✅ Автодеплой Production v5.1 завершен!"
          
    - name: Health check
      uses: appleboy/ssh-action@v1.0.3
      with:
        host: ${{ secrets.PRODUCTION_HOST }}
        username: ${{ secrets.PRODUCTION_USER }}
        key: ${{ secrets.PRODUCTION_SSH_KEY }}
        port: 22
        timeout: 60s
        command_timeout: 10m
        script: |
          echo "🔍 Выполняем health check..."
          sleep 20
          
          # Проверяем PM2 процессы
          echo "🔍 Проверяем PM2 процессы..."
          pm2 list
          
          # Проверяем backend
          echo "📡 Проверяем backend API..."
          for i in {1..5}; do
            echo "Попытка $i из 5..."
            if curl -f http://localhost:3002/api/health; then
              echo "✅ Backend работает!"
              break
            else
              echo "❌ Попытка $i не удалась"
              if [ $i -eq 5 ]; then
                echo "❌ Backend не отвечает после 5 попыток"
                echo "📋 Логи PM2:"
                pm2 logs northrepublic-backend --lines 10 || echo "❌ PM2 логи недоступны"
                exit 1
              fi
              sleep 5
            fi
          done
          
          # Проверяем MongoDB через health endpoint
          echo "🔗 Проверяем MongoDB..."
          health_response=$(curl -s http://localhost:3002/api/health)
          if echo "$health_response" | grep -q '"mongodb":"connected"'; then
            echo "✅ MongoDB подключена!"
          else
            echo "❌ MongoDB не подключена"
            echo "Ответ health endpoint: $health_response"
            exit 1
          fi
          
          # Проверяем frontend
          echo "🌐 Проверяем frontend..."
          if curl -f https://northrepublic.me | grep -q "North Republic"; then
            echo "✅ Frontend работает!"
          else
            echo "❌ Frontend не отвечает"
            exit 1
          fi
          
          # Проверяем bot
          echo "🤖 Проверяем Telegram bot..."
          bot_status=$(pm2 list | grep northrepublic-bot | awk '{print $10}')
          if [ "$bot_status" = "online" ]; then
            echo "✅ Bot работает!"
          else
            echo "❌ Bot не работает (статус: $bot_status)"
            echo "📋 Логи bot:"
            pm2 logs northrepublic-bot --lines 5 || echo "❌ PM2 логи недоступны"
            exit 1
          fi
          
          echo "🎉 Все проверки пройдены успешно!"
          
    - name: Notify success
      if: success()
      run: |
        echo "✅ Деплой успешно завершен!"
        echo "🌐 Сайт: https://northrepublic.me"
        echo "🔧 Админ панель: https://northrepublic.me/admin"
        echo "📡 Backend API: http://localhost:3002/api/health"
        
    - name: Notify failure
      if: failure()
      run: |
        echo "❌ Деплой завершился с ошибкой!"
        echo "Проверьте логи выше для деталей"
        echo "🔧 Для ручной проверки: ssh nr 'cd www/northrepublic.me && pm2 list && pm2 logs'"
