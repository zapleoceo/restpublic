# Правила разработки


Вместо фала .env используй env.txt Файл должен находиться в папке cursor 

## 🌍 Окружение

### Production (Продакшен)
- **Домен**: https://northrepublic.me
- **Сервер**: 159.253.23.113
- **Пользователь**: northrepubli_usr
- **Путь**: /var/www/northrepubli_usr/data/www/northrepublic.me
- **Деплой**: Автоматический через GitHub Actions при push в main
- **Команда**: `ssh nr`

## 🌿 Git ветки

### Ветка `main` (Production)
- **Назначение**: Продакшен версия
- **Деплой**: Автоматический через GitHub Actions при push в main
- **Правило**: ВСЯ разработка ведется в ветке main
- **Содержимое**: Стабильные, протестированные изменения

## Общие правила
- Всегда пиши по-русски
- **ВАЖНО: ВСЯ разработка ведется ТОЛЬКО на локалке**
- На локалке ничего не запущено (серверы не запускаем)
- Все запросы к API делай через удаленный сервер
- Всегда указывай токен в запросах к API
- **НИКОГДА не редактируем файлы напрямую на сервере**

## Процесс разработки
1. **Переключение на main**: `git checkout main` (всегда работаем в main)
2. **Разработка на локалке**: Все изменения делаются локально в коде
3. **Повышение версий**: Обязательно перед коммитом
4. **Тестирование**: Проверяем код локально (сборка, линтинг)
5. **Коммит и пуш в main**: `git add . && git commit -m "описание" && git push origin main`
6. **Ручной деплой на сервере**: Подключаемся к серверу и запускаем `./deploy.sh`
7. **Проверка на Production**: Проверяем работу на https://northrepublic.me
8. **Отладка**: При ошибках проверяем логи на сервере

## Процедуры деплоя и отладки

### Деплой
**ВАЖНО: Автодеплой через GitHub Actions НЕ РАБОТАЕТ!**

**Ручной деплой на сервере**:
1. Подключаемся к серверу: `ssh nr`
2. Переходим в директорию проекта: `cd /var/www/northrepubli_usr/data/www/northrepublic.me`
3. Запускаем скрипт деплоя: `./deploy.sh`

**Что делает скрипт**:
1. Очищает все изменения на сервере
2. Обновляет код с Git (git pull origin main)
3. Устанавливает зависимости backend
4. Собирает frontend
5. Копирует файлы в корень проекта
6. Перезапускает сервисы через PM2

**Использование**: Запустить на сервере из корня проекта `./deploy.sh`

### Проверка логов
- **Backend логи**: `ssh nr "pm2 logs northrepublic-backend --lines 50"`
- **Bot логи**: `ssh nr "pm2 logs northrepublic-bot --lines 50"`
- **Все процессы**: `ssh nr "pm2 logs --lines 30"`
- **Файловые логи**: `ssh nr "tail -f /var/www/northrepubli_usr/data/www/northrepublic.me/logs/backend.log"`

### Перезапуск сервисов
- **Backend**: `ssh nr "pm2 restart northrepublic-backend"`
- **Bot**: `ssh nr "pm2 restart northrepublic-bot"`
- **Все сервисы**: `ssh nr "pm2 restart all"`
- **После перезапуска**: Всегда проверить статус `pm2 list`

### Отладка частых ошибок
- **Ошибка 500 при заказе**: Проверить логи backend, возможно проблема с модификаторами товаров
- **Сессия не сохраняется**: Проверить localStorage ключ `user_session`, перезагрузить страницу
- **Бот не отвечает**: Перезапустить bot сервис, проверить токен в .env
- **API не работает**: Проверить nginx конфигурацию, порт 3002
- **"modificatorId is undefined"**: Товар требует модификаторы, но они не переданы в запросе

## ⚠️ ВАЖНО: Правила работы с Git ветками

### ✅ РАЗРЕШЕНО:
- **ВСЯ разработка** только в ветке `main`
- **Пуш в main**: `git push origin main` (автодеплой на production)
- **Работа в main ветке** для всей разработки

### 🔄 Правильный процесс:
1. Работаем в `main` ветке
2. Пушим в `main`: `git push origin main`
3. Автодеплой на production происходит автоматически

## Работа с серверами
- **Production (northrepublic.me)**: Автоматический деплой через GitHub Actions при push в main
- **ТОЛЬКО для проверки**: Подключение к серверу для просмотра логов и статуса
- **НЕ редактируем**: Файлы на сервере не изменяем
- **НЕ копируем**: Файлы не копируем на сервер вручную
- **ТОЛЬКО pull**: На сервере выполняем git pull для получения изменений

## Безопасность
- НИКОГДА не добавляй пароли, токены, ключи в Git
- Все чувствительные данные только в .cursor/env.txt
- Проверяй каждый коммит на наличие секретов
- Используй git-secrets или подобные инструменты

## Git правила
- Всегда пиши коммиты на русском языке
- Используй форс-пуш при необходимости
- Папка .cursor в .gitignore
- НЕ изменяй .gitignore без необходимости
- НЕ создавай дополнительные .gitignore файлы
- ПЕРЕД каждым push повышай версии в package.json backend и frontend
- ПЕРЕД каждым push повышай APP_VERSION в ecosystem.config.js для проверки обновлений

### 🌿 Работа с ветками:
- **ВСЯ разработка** только в ветке `main`
- **Автодеплой** на production при push в main

## Компоненты и код
- НЕ создавай лишние компоненты без ТЗ
- НЕ добавляй ненужные зависимости
- Следуй существующей архитектуре проекта
- Код должен быть минимальным и функциональным

## Работа с ценами
- Цены от API Poster нужно делить на 100 везде
- Проверяй цены через запрос к серверу с токеном
- Не забывай указывать токен в запросах

## Работа с Telegram ботом

### Основная информация
- **Основной бот**: @RestPublic_bot
- **Токен**: Хранится в .env на сервере (TELEGRAM_BOT_TOKEN)
- **Процесс**: northrepublic-bot (управляется через PM2)
- **Логи**: `ssh nr "pm2 logs northrepublic-bot"`

### Конфигурация бота
- **Команды**: Минимальный набор (только /start для авторизации)
- **Встроенное меню**: ОТКЛЮЧЕНО (`bot.telegram.deleteMyCommands()`)
- **Основные кнопки**: Только "📱 Авторизоваться" (с запросом контакта)
- **Никаких лишних команд**: /categories, /help и другие УДАЛЕНЫ

### Логика авторизации в боте
1. **Команда /start**: Показывает кнопку "📱 Авторизоваться"
2. **Кнопка авторизации**: Запрашивает контакт пользователя (`Markup.button.contactRequest`)
3. **Обработка контакта**: Отправляет данные на backend через `/api/auth/telegram-callback`
4. **Возврат пользователя**: Показывает кнопку "🔗 Вернуться в приложение" с правильным URL

### Правила разработки бота
- **НЕ добавлять лишние команды**: Только авторизация
- **НЕ добавлять встроенное меню**: Использовать только custom кнопки
- **Простота интерфейса**: Минимум кликов для пользователя
- **Правильный возврат**: Бот должен возвращать на исходную страницу сайта

### Отладка бота
- **Перезапуск**: `ssh nr "pm2 restart northrepublic-bot"`
- **Логи ошибок**: `ssh nr "pm2 logs northrepublic-bot --lines 50"`
- **Проверка токена**: Убедиться, что TELEGRAM_BOT_TOKEN корректный в .env
- **Тестирование**: Проверить авторизацию через бота на реальном устройстве

## Серверы и деплой

### Production (northrepublic.me)
- Сервер: 159.253.23.113
- Пользователь: northrepubli_usr
- SSH ключ: ~/.ssh/northrepublic (локально)
- Путь на сервере: /var/www/northrepubli_usr/data/www/northrepublic.me
- **Деплой**: Автоматический через GitHub Actions при push в main
- **Доступы**: Указаны в .cursor/env.txt
- База данных: northrepubli
- **Порт backend**: 3002
- **Nginx конфигурация**: API проксируется на порт 3002
- Логи: logs/backend.log и logs/bot.log
- PM2: доступен для управления процессами
- **Cloudflare**: Проект защищен Cloudflare, SSL сертификаты от Cloudflare

## Подключение к серверу (только для проверки)
- **Production**: `ssh nr` (использует ~/.ssh/config)
- **ТОЛЬКО для просмотра**: pm2 list, pm2 logs, tail -f logs/*.log
- **НЕ редактируем**: Файлы на сервере не изменяем

## GitHub Actions
- **Production**: Автодеплой при push в main ветку
- Секреты: PRODUCTION_HOST, PRODUCTION_USER, PRODUCTION_SSH_KEY
- Workflow: .github/workflows/deploy.yml
- Health check после деплоя
- **Автоматически**: Pull кода, установка зависимостей, сборка, перезапуск сервисов

## Отладка и проверка

### Production (northrepublic.me)
- **Проверка API**: https://northrepublic.me/api/health
- **Проверка меню**: https://northrepublic.me/m
- **Логи backend**: `ssh nr "tail -f /var/www/northrepubli_usr/data/www/northrepublic.me/logs/backend.log"`
- **Логи bot**: `ssh nr "tail -f /var/www/northrepubli_usr/data/www/northrepublic.me/logs/bot.log"`
- **Статус процессов**: `ssh nr "pm2 list"`
- **Перезапуск backend**: `ssh nr "pm2 restart northrepublic-backend"`
- **Перезапуск bot**: `ssh nr "pm2 restart northrepublic-bot"`

## Особенности Poster API

### Работа с клиентами
- **Автоматическое создание**: Клиенты создаются через `clients.createClient`
- **Поиск по телефону**: Используется `clients.getClients` с фильтром по phone
- **Формат телефона**: Убираются все символы кроме цифр для поиска
- **Обязательные поля**: name, phone (минимум для создания клиента)

### Система заказов
- **Активные заказы**: Статус 0 (новый) или 1 (в процессе)
- **Временное окно**: Поиск активных заказов за последние 6 часов
- **Объединение заказов**: Новые товары добавляются к активному заказу через `updateIncomingOrder`
- **Spot ID**: Всегда используется spot_id = 1 (RestPublica)

### Обработка модификаторов
- **Проверка модификаторов**: Через `menu.getProductModificators`
- **Обязательные модификаторы**: Если у товара есть модификаторы, они могут быть обязательными
- **Формат для API**: `{modificator_id: 123, modificator_products: [456, 789]}`
- **Ошибка "modificatorId is undefined"**: Товар требует модификаторы, но они не переданы

### Работа с ценами
- **Цены в API**: Всегда в копейках (умножить на 100 при отправке)
- **Отображение**: Делить на 100 для показа пользователю
- **Нормализация**: Backend автоматически нормализует цены при получении меню

## База данных и конфигурация
- **Доступы к БД**: Указаны в .cursor/env.txt
- **Работа с БД**: Все операции через API endpoints
- **Кэширование**: Backend автоматически кэширует данные

## SePay мониторинг платежей
- **Production**: SePay мониторинг ВКЛЮЧЕН и работает
- **Логирование**: Минимальное логирование для уменьшения размера логов
- **Обработка ошибок**: Автоматическая остановка при критических ошибках
- **Управление**: Через API endpoints для мониторинга
- **Конфигурация**: SePay API токен в .env файле на сервере

## Версионирование
- **Перед каждым push**: Обязательно повышать версии
- **Backend**: package.json (версия 1.0.XX)
- **Frontend**: package.json (версия 2.2.XX)
- **PM2**: APP_VERSION в ecosystem.config.js (версия 2.3.XX)
- **Проверка обновлений**: Через /api/health endpoint
- **Синхронизация**: Версии должны быть синхронизированы между файлами

## Frontend и API
- **Работа с API**: Использовать хуки для получения данных
- **Статические файлы**: Не использовать, все данные через API
- **i18n**: Использовать кастомный ApiBackend



## Проверка после деплоя
- **Ожидание**: 30 секунд после push для автодеплоя
- **API health**: Проверка работоспособности API
- **Основные страницы**: Проверка корректной загрузки
- **Логи**: Проверка отсутствия ошибок в PM2 логах

## Портовая конфигурация
- **Production**: Порт 3002
  - Backend работает на порту 3002
  - Nginx проксирует API запросы на 127.0.0.1:3002
  - Конфигурация: ecosystem.config.js с `PORT: 3002`
- **Проверка портов**: `netstat -tulpn | grep :3002`

## Структура проекта
- **Архитектура**: Следовать существующей архитектуре проекта
- **Компоненты**: Не создавать лишние компоненты без необходимости
- **Зависимости**: Не добавлять ненужные зависимости
